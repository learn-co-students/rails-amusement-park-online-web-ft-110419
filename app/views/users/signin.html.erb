<%= form_for (action="/signin") do%>
    Your Name: <%= select_tag :user_name, options_from_collection_for_select(User.all, :id, :name) %>
    Your Password: <%= password_field_tag :password %>

    <%= submit_tag "Sign In" %>
<% end %>